{
  "name": "CypresswithTypescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  /*"scripts": {
    "report-status": "Complete report creation with merging into one report and Html file Generated",
    "pretest": "rimraf -r ./cypress/reports/*.json",
    "test":"npm run cypress-test || npm run posttest",
    "cypress-test":"cypress run",
    "posttest":"npm run merge-reports && npm run generate-reports && npm run index",
    "merge-reports": "mochawesome-merge ./cypress/reports/*.json > report1.json",
    "generate-reports": "marge ./report1.json --reportDir ./cypress/reports",
    "index":"start ./cypress/reports/report1.html"
  },*/
  "scripts": {
        "test:chrome": "cypress run --browser chrome",
        "combine:report": "mochawesome-merge ./mochawesome-report/*.json -o index.json",
        "generate:report": "marge index.json --reportDir public --assetsDir public/assets --reportPageTitle index.html"
    },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.7.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.2.1",
    "mochawesome-report-generator": "^6.2.0",
    "typescript": "^4.7.2"
  }
}
